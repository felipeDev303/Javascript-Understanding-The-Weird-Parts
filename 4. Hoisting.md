El **hoisting** es un comportamiento del motor de JavaScript donde **las declaraciones** (no las asignaciones) se “mueven” lógicamente al **inicio de su scope** antes de ejecutar el código.

En realidad **no se mueven físicamente**:
el motor las **registra** en la fase de creación del _Execution Context_ antes de ejecutar la línea 1 del programa.

---

## 🧠💻 **Definición breve**

> **Hoisting es el proceso por el cual JavaScript registra las declaraciones de variables y funciones antes de ejecutar el código, permitiendo que puedan usarse antes de ser declaradas.**

---

## ✔ ¿Qué se "hoistea" y cómo?

### ✔ `var`

- Se declara al inicio del scope.
- Valor inicial: **undefined**.

```js
console.log(a); // undefined
var a = 5;
```

### ✔ `function declaration`

- Se eleva **declaración completa** (incluye el cuerpo).
- Puede llamarse antes de estar escrita.

```js
saludar(); // "Hola"

function saludar() {
  console.log("Hola");
}
```

### ✖ `let` y `const`

- Sí se elevan, **pero permanecen en la “Temporal Dead Zone (TDZ)”**
  hasta que la ejecución alcanza su línea.

```js
console.log(b); // ❌ ReferenceError
let b = 10;
```

---

## 📦 ¿Cómo funciona internamente?

Durante la **creación del Execution Context**, el motor crea dos estructuras:

### 1. **Variable Environment (para `var`)**

Registra los `var` con valor **undefined**.

### 2. **Lexical Environment (para `let` / `const`)**

Registra los identificadores, pero **sin asignar valor aún**
→ esto crea la **TDZ**, un periodo donde la variable existe pero es inaccesible.

### 3. **Function Declarations**

Se guardan completas, por eso funcionan antes de su definición.

---

## 🧠 Analogía neurocientífica

Piensa en el **hoisting** como la manera en que tu cerebro:

### ✔ **Primero activa el “diccionario mental”** (asigna _existencia_ a conceptos)

Antes de comprender una frase, el cerebro:

1. Reconoce las palabras que _deberán aparecer_
2. Reserva espacio en la memoria de trabajo
3. Luego asigna significado cuando aparece el contenido

### En términos cognitivos:

| JS                                 | Cerebro                                                    |
| ---------------------------------- | ---------------------------------------------------------- |
| Declara `var x` → asigna undefined | Reconoce que “x” existe aunque no tenga significado aún    |
| Function declarations completas    | Reconoce patrones completos: “esta frase ejecuta acción X” |
| TDZ con `let/const`                | Concepto existe pero aún **no está activado** para su uso  |

Es como si el cerebro dijera:

- **“Esta variable existe”** (hoisting)
- **“Pero todavía no puedes usarla”** (TDZ)

---

## 🎯 Explicación corta para recordar

- **Las declaraciones se elevan**, las asignaciones no.
- `var` → hoisting + valor undefined
- `function` → hoisting completo
- `let` / `const` → hoisting + Temporal Dead Zone
- Ocurre antes de ejecutar la primera línea de código.

---

Anterior tema: [Global Environment & The Global Object](./3.%20Global%20Environment%20%26%20The%20Global%20Object.md)
Siguiente tema: [Code Execution](./5.%20Code%20Execution.md)
