## ⚙️ **Single-Threaded Model y Synchronous Execution en JavaScript**

Big Words Alert

JavaScript tiene un modelo de ejecución **simple pero muy particular**, y estos dos conceptos son la base:

- **Single-Threaded Model**
- **Synchronous Execution**

---

### 🧵 1. **Single-Threaded Model**

#### ✔ ¿Qué significa “single-threaded”?

Significa que **JavaScript solo tiene un hilo de ejecución principal**.
Solo puede hacer **una cosa a la vez**.

No puede ejecutar dos líneas de código simultáneamente ni correr funciones en paralelo (dentro del mismo hilo).

#### 🔍 Consecuencias

- Solo hay **un call stack**.
- Si una tarea tarda (ej: un while infinito), **bloquea todo el programa**.
- No hay carreras de datos internas en el hilo principal.

#### 📦 Ejemplo

```js
console.log("A");
console.log("B");
console.log("C");
```

Salida siempre:

```
A
B
C
```

---

### ⏱️ 2. **Synchronous Execution**

#### ✔ ¿Qué significa “synchronous”?

Significa que el motor **ejecuta cada línea en orden**,
y **no avanza a la siguiente** hasta que la actual termina.

Es un modelo **bloqueante**:
A debe terminar antes de que se empiece B.

#### 💡 Ejemplo

```js
function tareaLenta() {
  for (let i = 0; i < 2_000_000_000; i++) {}
}

console.log("Inicio");
tareaLenta(); // bloquea todo
console.log("Fin"); // solo aparece cuando termina la anterior
```

---

## 🧠 ¿Cómo se relacionan ambos conceptos?

**Single-Threaded** → solo hay un camino de ejecución.
**Synchronous** → ese camino se recorre paso por paso.

Juntos significan:

> JavaScript ejecuta el código en un único hilo y lo hace de manera bloqueante: una instrucción tras otra.

---

## 💡 ¿Y entonces cómo corre código asincrónico? (timers, fetch, promesas, etc.)

Porque **el navegador y Node.js agregan APIs externas** (Timers, Web APIs, Libuv):
esas tareas se hacen **fuera del hilo principal**, y luego regresan al Event Loop para ser procesadas.

👉 Pero **el motor JavaScript en sí sigue siendo single-threaded**.

---

## 🧠 Analogía neurocientífica (simple y clara)

### ✔ Single-Threaded

Como la **atención consciente humana**:
solo puedes _atender_ a una tarea cognitiva central a la vez.

### ✔ Synchronous Execution

Como seguir una receta **paso por paso**, sin saltarte ninguno hasta que lo terminas.

El cerebro tiene procesos paralelos, sí,
pero la **atención ejecutiva** que equivale al _call stack_ es estrictamente secuencial.

---

## 📝 Resumen rápido

| Concepto                  | Definición corta                                | Consecuencia                  |
| ------------------------- | ----------------------------------------------- | ----------------------------- |
| **Single-Threaded Model** | Un solo hilo de ejecución                       | No ejecuta código en paralelo |
| **Synchronous Execution** | Cada instrucción debe terminar antes de avanzar | Ejecución bloqueante          |

---

Anterior tema: [Hoisting](./4.%20Hoisting.md)
Siguiente tema: [Scope & Scope Chain](./5.%20Scope%20%26%20Scope%20Chain.md)
